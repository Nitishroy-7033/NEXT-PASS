import 'package:flutter/material.dart';

class WebDatabaseSetup extends StatelessWidget {
  const WebDatabaseSetup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}