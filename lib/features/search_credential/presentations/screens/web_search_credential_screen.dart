import '../../../../../core/constants/app_linker.dart';

class WebSearchCredentialScreen extends StatelessWidget {
  const WebSearchCredentialScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}